server:
  port: 8082
spring:
  application:
    name: catalog-service
  mongodb:
    uri: ${SPRING_MONGODB_URI:${SPRING_DATA_MONGODB_URI:${MONGODB_URI:mongodb://admin:admin123@localhost:27017/catalog_db?authSource=admin}}}
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${USER_SERVICE_URL:http://localhost:8081}/.well-known/jwks.json

catalog:
  seed:
    enabled: ${CATALOG_SEED_ENABLED:false}
  security:
    enabled: ${CATALOG_SECURITY_ENABLED:false}
    jwt:
      public-key-location: ${JWT_PUBLIC_KEY_LOCATION:}
      jwk-set-uri: ${JWT_JWK_SET_URI:}
  image:
    max-size-bytes: ${CATALOG_IMAGE_MAX_SIZE_BYTES:1024000} # 10 MB
    allowed-content-types:
      - image/jpeg
      - image/png
      - image/webp

minio:
  endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
  access-key: ${MINIO_ACCESS_KEY:minioadmin}
  secret-key: ${MINIO_SECRET_KEY:minioadmin}
  bucket: ${MINIO_BUCKET:book-images}
  secure: ${MINIO_SECURE:false}
